services:
  - type: web
    name: hazard-report-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: ADMIN_PASSWORD
        value: admin123
      - key: GITHUB_TOKEN
        sync: false
